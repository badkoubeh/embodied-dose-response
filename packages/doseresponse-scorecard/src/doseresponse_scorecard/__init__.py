"""Domain-neutral dose-response threshold estimation.

Shared with ZetaBench. **Nothing model-, policy-, or domain-specific may enter
this package** (PLAN.md 3.2) -- it sees severities, outcomes, and cluster ids,
and nothing else. That constraint is what lets the same protocol describe a
driving policy and a PID loop reading a biased state estimate.

STUB: the API below is pinned, the implementations are not written.
"""

__version__ = "0.0.1"
