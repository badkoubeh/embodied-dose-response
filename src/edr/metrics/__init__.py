"""Scoring: pure functions over RawRecord (PLAN.md 3.2).

STUB. The extractor cannot be written before the calibration set exists
(PLAN.md 5, Stage 1) -- writing it first would mean tuning it on the data it is
supposed to be validated against.

Everything here is CPU-only and re-runnable against stored raw artifacts, which
is the payoff of the PLAN.md 2.1 plane split.
"""
